# clone the code

name: Code-clone

on:
  push:
  pull_request:
    branch:
      - master

jobs:
  code-checkout:
    name: code checkout  
    runs-on: ubuntu-latest

    steps:
      - name: git checkout
        uses: actions/checkout@v4
        with:
          token: ${{secrets.MY_GITHUB_TOKEN}}

